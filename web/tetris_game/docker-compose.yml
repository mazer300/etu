version: '3.8'
services:
  tetris:
    image: tetris-game
    build: .
    container_name: tetris-game
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
    volumes:
      - ./:/usr/share/nginx/html
    restart:
      unless-stopped