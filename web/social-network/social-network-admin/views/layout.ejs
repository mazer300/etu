<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Подключаем стили в зависимости от типа сборки -->
    <% if (buildType === 'gulp') { %>
        <link href="/dist-gulp/css/main.min.css" rel="stylesheet">
    <% } else { %>
        <link href="/dist-webpack/css/main.css" rel="stylesheet">
    <% } %>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Социальная сеть</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/users?build=<%= buildType %>">Пользователи</a>
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        Сборка: <%= buildType %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="?build=gulp">Gulp сборка</a></li>
                        <li><a class="dropdown-item" href="?build=webpack">Webpack сборка</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <%- body %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Подключаем скрипты в зависимости от типа сборки -->
    <% if (buildType === 'gulp') { %>
        <script src="/dist-gulp/js/main.min.js"></script>
    <% } else { %>
        <script src="/dist-webpack/js/main.js"></script>
    <% } %>
</body>
</html>